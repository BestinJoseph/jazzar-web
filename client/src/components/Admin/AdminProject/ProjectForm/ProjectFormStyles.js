import { makeStyles } from "@material-ui/core"

export default makeStyles((theme) => ({
    projectform: {
        // marginTop: '2rem',
        padding: '2rem',
        width: '70%',

        '& .projectFormTextFieldCover': {
            margin: '1rem 0'
        }
    }
}))